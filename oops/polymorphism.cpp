//one class with differnt forms like one class can be parent to one and child of other class
// function overloading: functions name same but different parameters - polymorphism
//operator overloading like + adds strings and integers differently - polymorphism
// in a class there are two objects how to do polymorphic operations
//we can also use operators as a functions - polymorphism

